---
import DocsLayout from '#layouts/Docs.astro'
import { getDocStaticPaths } from '#lib/docsPost.ts'

export const getStaticPaths = getDocStaticPaths('en')

const { post } = Astro.props
---

<DocsLayout {post} locale="en" />
